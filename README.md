# acoustic-communication
Two machines exchange text messages through sound. Characters are encoded into bits, each transmitted as a precise audio frequency (FSK modulation). The receiver captures audio, detects frequencies via the Goertzel algorithm, and reconstructs the original message — no network required, just sound.
